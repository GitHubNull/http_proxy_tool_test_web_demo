# HTTP/WebSocket代理测试工具 - 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# 构建相关配置
BUILD_VERSION=v1.0.0
BUILD_TIME=2024-01-01T00:00:00Z
BUILD_COMMIT=latest
TAG=latest

# 应用配置
GIN_MODE=release
HOST_PORT=8080
TIMEZONE=Asia/Shanghai
LOG_LEVEL=info

# Nginx 配置（可选）
NGINX_PORT=80
NGINX_SSL_PORT=443

# 监控配置（可选）
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_PASSWORD=admin

# 数据路径配置
LOGS_PATH=./logs
CONFIG_PATH=./config

# 生产环境特定配置
# 如果需要外部数据库或其他服务，在这里添加配置
# DATABASE_URL=postgresql://user:password@host:port/dbname
# REDIS_URL=redis://host:port/db 